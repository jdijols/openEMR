import type { ReactElement } from 'react';

export default function App(): ReactElement {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '1rem' }}>
      <h1>AgentForge CUI</h1>
      <p>Gate 0 scaffold — chat UI lands in Gate 2.</p>
    </main>
  );
}

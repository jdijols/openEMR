---
title: Clinical Copilot — W2 Implementation Tasks (Multimodal Evidence Agent)
architecture: W2_ARCHITECTURE.md
prior_week: W1_ARCHITECTURE.md
brief: Documentation/AgentForge/references/Week 2 - AgentForge Clinical Co-Pilot.pdf
deadline: 2026-05-10T12:00:00 CT (Gauntlet AgentForge W2 final submission)
created: 2026-05-04
mvp_gate: 2026-05-05T23:59:00 CT
early_gate: 2026-05-07T23:59:00 CT
final_gate: 2026-05-10T12:00:00 CT
---

# Clinical Copilot — W2 Implementation Tasks

> Built on the W1 fork. Multimodal Evidence Agent: seeing clinical documents, routing work, and gating changes with evals.
>
> This document is the **execution map** for W2, rooted in [`W2_ARCHITECTURE.md`](W2_ARCHITECTURE.md). Tasks are ordered by **ship gate** (dependency-first), not by architecture section order. Every task points back to its architecture section so anything here can be re-derived from the spec.
>
> **Examiner grading — the hard gate is the eval gate.** Brief: *"During grading, we will introduce a small regression and confirm your CI gate fails. If the eval gate does not block the regression, the Week 2 build does not pass."* See **G2-Early-Eval** + **G2-Final-Rehearsal** rows. The Saturday self-injection rehearsal is mandatory practice for grading day.
>
> **Prior week's task list (archived):** [`Documentation/AgentForge/archive/TASKS_W1.md`](Documentation/AgentForge/archive/TASKS_W1.md).

## How to read this file

- **Gates run in dependency order.** Within a gate, sub-tasks may run in parallel where the **Depends on** column allows.
- **Tests are not symmetric.** Security and agent-correctness tasks are **test-first** (write the unit/integration test before or with the code). UI rendering, scaffolding, and infra are **smoke + manual demo**, not test-first.
- **Cut tier** = which scope tier this task can be dropped to recover schedule. `MUST` means the task cannot be cut without violating §1 anti-success criteria of [`W2_ARCHITECTURE.md`](W2_ARCHITECTURE.md). Lower tiers cut earlier (tier 1 first, tier 6 last).
- **Done proof** is the literal artifact, command output, or scenario name a reviewer can re-check. Vague proofs are bugs in this file.
- **Owner** column intentionally omitted — single engineer + AI pair.
- **Status markers:** `[ ]` open · `[~]` in progress · `[x]` done with proof · `[-]` cut per a tier (note tier in line).

## Stop-the-line tests (non-negotiable)

If any of these fail at any point, stop feature work and fix before continuing. Lines S1–S10 carry forward from W1 unchanged. Lines S11–S15 are W2-specific, rooted in [`W2_ARCHITECTURE.md` §13](W2_ARCHITECTURE.md) (security & PHI posture) and §14 (risks).

| #   | Invariant                                                                                          | Where enforced                                              | First test gate    |
| --- | -------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | ------------------ |
| S1  | No cross-patient leak (active-chart binding holds, both layers — extended to upload + DocBytes)    | W2_ARCHITECTURE §4, §13; W1 carry-forward                   | G2-MVP             |
| S2  | No silent write (every write preceded by explicit confirm — extended to intake proposal card)      | W2_ARCHITECTURE §9, §10                                     | G2-MVP             |
| S4  | No copilot privilege bypass: OpenEMR session/GACL + active-chart binding govern access             | W2_ARCHITECTURE §13; W1 carry-forward                       | G2-MVP             |
| S5  | No tokens in URLs, browser history, referrer, or access logs                                       | W1 carry-forward                                            | G2-MVP             |
| S6  | No raw exception messages in any client-visible response                                            | W1 carry-forward                                            | G2-MVP             |
| S7  | No PHI in Langfuse trace bodies (deny-list redaction at wire boundary)                             | W1 carry-forward; W2_ARCHITECTURE §12                       | G2-MVP             |
| S8  | No uncited clinical claim ships to the CUI                                                          | W1 carry-forward; W2_ARCHITECTURE §11 `citation_present`    | G2-MVP             |
| S9  | No write target outside the supported enum is callable                                             | W1 carry-forward + W2 write-tool additions                  | G2-Early           |
| S10 | No PHI in OpenEMR or agent API logs (metadata only)                                                 | W1 carry-forward                                            | G2-MVP             |
| **S11** | **No raw document bytes / extracted text / structured extraction JSON in any Langfuse span body** | W2_ARCHITECTURE §12 hard rule; rubric `no_phi_in_logs`       | **G2-MVP**         |
| **S12** | **Eval gate (Git Hook + CI) MUST fail on >5pp regression OR sub-95% absolute, in any of 5 categories** | W2_ARCHITECTURE §11; brief's hard gate                  | **G2-Early**       |
| **S13** | **FHIR `DocumentReference` idempotency holds — same `(patient_uuid, sha256)` yields one row, no duplicates** | W2_ARCHITECTURE §10                                | **G2-MVP**         |
| **S14** | **PDF cross-check is gating — facts whose `quote_or_value` is not present in `pdf-parse` text are NOT persisted** | W2_ARCHITECTURE §5, §10                                  | **G2-MVP**         |
| **S15** | **DocBytes endpoint refuses cross-patient document fetch — `DocRef.subject != active_chart_uuid` returns 403** | W2_ARCHITECTURE §4, §13                                  | **G2-MVP**         |

A green check on every stop-the-line test is a precondition for Sun 12:00 final-gate sign-off.

## Gate dependency graph

```
                    ┌───────────────────────────────────────────────┐
                    │  G2-MVP — Local-runnable demo (Tue 11:59 PM)  │
                    │  · Cohort patients seeded                     │
                    │  · Composer + upload + extraction E2E         │
                    │  · Read-only intake proposal card             │
                    │  · Lab auto-persist                           │
                    │  · First-cut RAG (small corpus, demo-able)    │
                    │  · Stop-the-line: S1, S2, S5, S6, S7, S8,     │
                    │    S10, S11, S13, S14, S15 green              │
                    └───────────────────────┬───────────────────────┘
                                            │
                    ┌───────────────────────▼───────────────────────┐
                    │  G2-Early — Deployed + Eval Gate (Thu 11:59)  │
                    │  · Supervisor refactor (handoff spans)        │
                    │  · 4 new W2 write tools + delete_uploaded_doc │
                    │  · Intake proposal dispatches on confirm      │
                    │  · 50-case eval suite + 3 new check types     │
                    │  · baseline.json pinned                       │
                    │  · Local Git Hook + GH Actions PR-blocking    │
                    │  · VPS deployed; bbox overlay live            │
                    │  · Demo video v1 (rough cut)                  │
                    │  · Stop-the-line S12 + S9 green               │
                    └───────────────────────┬───────────────────────┘
                                            │
                    ┌───────────────────────▼───────────────────────┐
                    │  G2-Final — Submission (Sun 12:00 PM)         │
                    │  · Per-field edit on intake proposal card     │
                    │  · propose_demographics_update                │
                    │  · Sat self-injection rehearsal (S12 verify)  │
                    │  · Cost & latency report                      │
                    │  · Demo video final cut                       │
                    │  · W2_ARCHITECTURE.md final pass              │
                    │  · Submission package complete                │
                    └───────────────────────────────────────────────┘

Cross-cutting / continuous (every gate, not gate-resident):
   · No PHI in Langfuse — verified by `no_phi_in_logs` rubric every commit
   · W1 evals stay green — no UC-A through UC-J regression
   · Conventional commits + `Assisted-by: Claude Code` trailer
   · Daily journal entry under Documentation/AgentForge/process/journal/week-2/
   · Working tree stays clean of unrelated drift
```

## Schedule overlay

| Wall date  | Day      | Gate target                       | Cut-tier trigger if not met                                |
| ---------- | -------- | --------------------------------- | ---------------------------------------------------------- |
| Mon 5/4    | Today-1  | Architecture Defense (✅ done)    | n/a                                                        |
| Tue 5/5    | MVP day  | G2-MVP complete by 11:59 PM CT    | Slip → drop bbox overlay to G2-Early (tier 2 cut)          |
| Wed 5/6    | Build    | Supervisor refactor + write tools | Slip → drop demographics tool from Final (tier 3 cut)      |
| Thu 5/7    | Deploy   | G2-Early complete by 11:59 PM CT  | Slip → cut tier 1 polish; demo video v1 still ships        |
| Fri 5/8    | Polish   | Per-field edit + cost report      | Slip → drop per-field edit to "Final-if-time" (tier 1 cut) |
| Sat 5/9    | Rehearse | Self-injection rehearsal (S12)    | Slip rehearsal → grading day = unrehearsed (HIGH RISK)     |
| Sun 5/10   | Submit   | G2-Final by 12:00 PM CT           | Slip submission window → fail final gate                   |

---

## Gate G2-MVP — Local-runnable demo (Tuesday 2026-05-05 11:59 PM CT)

**Status:** `[ ]` OPEN

**Exit criteria:** end-to-end flow works for one cohort patient, locally — open chart → upload intake form → confirm proposal → upload lab → ask "should we intensify her statin?" → grounded answer with patient + evidence citations. Stop-the-line tests S1, S2, S5, S6, S7, S8, S10, S11, S13, S14, S15 green. Deployed app + 50-case eval + supervisor refactor + new write tools all defer to G2-Early.

### G2-MVP — Cohort patient seeding (BLOCKER for everything else)

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[x]` G2-MVP-01 | Verify Whitaker + Kowalski first names against PDF text (read full content); update if real names differ from provisional `Robert` / `Anna` | §10 (cohort) | — | None (manual: pdftotext both files, grep for first-name patterns) | High | Verified 2026-05-05: Whitaker → **James** (p02-whitaker-intake.pdf header "James E. Whitaker", DOB 1958-11-03, MRN-2026-04492); Kowalski → **Robert** (p04-kowalski-cmp.pdf "KOWALSKI, ROBERT", DOB 1971-06-08, MRN-2026-04518; corroborated by p04 intake PNG signature). W2_ARCHITECTURE.md §10 cohort table + open-questions log updated; ⓟ legend removed. Seeding-script comment to be added at G2-MVP-05. | MUST |
| `[x]` G2-MVP-01b | **Verify clinical fact alignment for smoke test (G2-MVP-99 dependency).** Read PDFs to confirm: (a) Chen lipid panel has ~5 results including LDL ~158 with ≥4 outside reference range, (b) Chen intake form lists T2DM (or equivalent diabetic marker that supports the smoke-test question). If facts don't match, swap fixture, update G2-MVP-99 step 9 question, or update `W2_ARCHITECTURE.md` §16 to match. Smoke test depends on demo question and PDF content being aligned. | §10 (cohort) | G2-MVP-01 | None (manual: `pdftotext Chen-Margaret-Lab-Lipid-Panel.pdf -`; `pdftotext Chen-Margaret-Intake-Form.pdf -`; grep for LDL/HDL/cholesterol and diabetes/T2DM/glucose) | MUST | Verified 2026-05-05: Chen lipid panel = 5 results — TC 232 (H), HDL 48 (L), LDL 158 (H), TG 178 (H), Non-HDL 184 (H), so 5/5 outside reference range (exceeds ≥4 spec). Chen intake = T2DM in active problems (E11.9 / SNOMED 44054006), current Metformin 500 mg PO BID since 2020, mother with T2DM in family history. Lipid panel interpretive comments include "Per 2018 ACC/AHA Cholesterol Clinical Practice Guideline, consider intensification of statin therapy in patients with diabetes mellitus and ASCVD risk factors" — direct semantic match for G2-MVP-99 step 9. **No fixture or smoke-question changes needed.** | MUST |
| `[x]` G2-MVP-02 | Rename 8 sample documents to medical-office convention `{LastName}-{FirstName}-{DocumentClass}-{SubType}.{ext}` | §10 (file naming) | G2-MVP-01 | None (manual: `ls Documentation/AgentForge/assets/W2-documents/{intake-forms,lab-results}/` shows the new pattern) | MUST | Renamed 2026-05-05 via `git mv` (history-preserving); `git diff --cached --stat` shows 8 R's with 0 insertions / 0 deletions. New names: `Chen-Margaret-{Intake-Form.pdf, Lab-Lipid-Panel.pdf}`, `Whitaker-James-{Intake-Form.pdf, Lab-CBC.pdf}`, `Reyes-Sofia-{Intake-Form.png, Lab-HbA1c.png}`, `Kowalski-Robert-{Intake-Form.png, Lab-CMP.pdf}`. All p0X- paths gone. | MUST |
| `[x]` G2-MVP-03 | Add `Documentation/AgentForge/assets/W2-documents/README.md` noting these are synthetic, not for any real workflow, listing the 4 patients and which file belongs to which patient | §10 (cohort) | G2-MVP-02 | None (manual review) | High | Written 2026-05-05 at `Documentation/AgentForge/assets/W2-documents/README.md` (~50 lines). Opens with a synthetic-data warning. Includes per-patient table (Chen / Whitaker / Reyes / Kowalski) mapping each to intake form + outside lab paths and noting Chen's lipid panel as the demo's headline document. Mentions naming convention + cross-references W2_ARCHITECTURE.md §10 + TASKS.md G2-MVP-01..07. | MUST |
| `[x]` G2-MVP-04 | Commit cohort sample documents to repo (move out of `.gitignore` if present; verify with `git status`) | §10 (cohort) | G2-MVP-03 | None (manual: `git ls-files Documentation/AgentForge/assets/W2-documents/` lists all 8 files + README) | MUST | Committed 2026-05-05 as `afcd330d7` ("docs(agentforge): W2 cohort doc prep + MVP plan gap fixes"). 11 files: 8 renames at 100% similarity + new README + updated TASKS.md + W2_ARCHITECTURE.md. Files were already tracked (not gitignored). User's local sqlconf/cert/log artifacts intentionally left untouched. | MUST |
| `[x]` G2-MVP-05 | Extend `contrib/util/agentforge/seed_cohort.php` with 4 new patients (Chen, Whitaker, Reyes, Kowalski). Demographics from intake form / lab metadata where available; sex defaulted/inferred where not. **Empty charts — no prior encounters, no prior vitals, no prior problem list, no prior medications, no prior allergies.** | §10 (cohort) | G2-MVP-01 | PHPUnit (or smoke): seeder is idempotent (re-run does not duplicate); each patient has one row in `patient_data` | MUST | Code added 2026-05-05 — 4 entries appended after Grace Kim as AF-COHORT-011..014. Demographics extracted from PDFs + ImageMagick-cropped PNGs (Chen 1967-08-14, Whitaker 1958-11-03, Reyes 1983-12-19, Kowalski 1971-06-08). Each entry has `'history' => []`, `'allergies' => []`, `'visits' => af_visits([])` — the chart starts empty until intake-form upload. Source filename referenced inline in each `theme`. Two-line edit at seedPatient lines 362-363 guards `first_date`/`last_date` for empty visits (prints `—`). `php -l` clean. **Idempotency** relies on existing `clearExistingCohort()` mechanism (clears all AF-COHORT-* before seeding). Runtime smoke (re-run, count rows) bundled with G2-MVP-07's manual chart-empty verification — same docker session. | MUST |
| `[x]` G2-MVP-06 | Extend `contrib/util/agentforge/seed_appointments.php` to schedule 4 *new-patient* appointments for the demo day (status `scheduled`, reason `new patient visit`) | §10 (cohort) | G2-MVP-05 | Smoke: appointments visible in OpenEMR calendar for the demo day | MUST | Code added 2026-05-05 (3 surgical edits, +25/-7): (1) `EXPECTED_DEMO_PATIENT_COUNT` 28 → 32 to accommodate 4 new W2 patients; (2) `loadCohortPatients` now selects `genericval1` and marks AF-COHORT-011+ with `source='new'` (W1 cohort 001..010 stay 'cohort'); (3) `run()` splits cohort by source — W1 joins established pool, W2 joins new-patient pool alongside the 15 scheduled patients. The 4 W2 patients (newPool=19, new template slots=15) spill into the existing overflow logic which places them on day 4 (2026-05-04) at trailing time slots (15:45+) with `pc_catid=new_patient`. `php -l` clean. Live calendar smoke bundled with G2-MVP-07. | MUST |
| `[ ]` G2-MVP-07 | **Verify locally:** open OpenEMR, navigate calendar to the demo day, click into Margaret Chen's chart → chart is empty (no prior encounters, no vitals, no labs). Repeat for Whitaker, Reyes, Kowalski. | §10 (cohort) | G2-MVP-05, G2-MVP-06 | Manual smoke (capture in journal) | MUST | Journal entry under `Documentation/AgentForge/process/journal/week-2/` with screenshots of empty charts for all 4 patients | MUST |

### G2-MVP — Schemas & types

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[x]` G2-MVP-10 | Create `agentforge/api/src/schemas/extraction.ts` exporting `SourceCitationSchema`, `LabResultSchema`, `LabPdfExtractionSchema`, `IntakeFormSchema` verbatim from `W2_ARCHITECTURE.md` §6 | §6 | — | None at this step (G2-MVP-11 covers tests) | MUST | File created 2026-05-05 at `agentforge/api/src/schemas/extraction.ts` (~95 lines). Mirrors §6 verbatim with 4 named exports + 4 inferred TS types (`SourceCitation`, `LabResult`, `LabPdfExtraction`, `IntakeForm`). Single discrepancy: `quote_or_value` upgraded from bare `z.string()` to `z.string().min(1)` so test scenario #3 (empty-quote rejection) is enforceable; W2_ARCHITECTURE.md §6 updated in lock-step. `tsc --noEmit` clean for the new file (4 pre-existing W1 typecheck errors in `test/http/`, `test/stt/`, `test/transcripts/` are unrelated baseline drift, not introduced here). | MUST |
| `[x]` G2-MVP-11 | **(test-first)** Validation tests at `agentforge/api/test/schemas/extraction.test.ts` per W2_ARCHITECTURE §6 "Validation tests": (1) round-trip, (2) missing-citation rejection, (3) empty quote_or_value rejection, (4) source_type enum violation, (5) bbox arity rejection | §6, §11 (`schema_valid`) | G2-MVP-10 | Vitest: 5 scenarios pass | MUST (S8 boundary) | Written 2026-05-05 at `agentforge/api/test/schemas/extraction.test.ts`. `npx vitest run test/schemas/extraction.test.ts` reports **6/6 green** in 3 ms (5 required scenarios in `§6 extraction schema validation` describe + 1 bonus IntakeForm round-trip smoke). Output: `Test Files 1 passed (1) / Tests 6 passed (6)`. | MUST (S8 boundary) |

### G2-MVP — Module / PHP layer (upload + DocRef + bytes proxy)

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[x]` G2-MVP-20 | **(test-first)** `OpenEMR\Modules\AgentForge\Documents\DocumentUploadAction` — accepts multipart, validates ACL (`agentforge/use`) + active-chart binding, computes SHA-256, idempotent insert into `documents` table, mints `DocumentReference` UUID via `UuidRegistry`, writes audit row `action='doc_upload'` with PHI-safe payload | §4, §10, §13 | G1 carry-forward (W1 module) | PHPUnit isolated: (a) upload happy path, (b) ACL fail → 403, (c) cross-patient binding fail → 403 (S1+S15), (d) re-upload same `(patient, sha256)` returns existing DocRef (S13), (e) audit row written with no patient name/MRN/DOB | MUST (S1, S13, S15) | Built 2026-05-05 — 6 module files under `interface/modules/custom_modules/oe-module-agentforge/src/Documents/` (Payload, Port, Result, AuditSink, Action, CrossPatientBindingException) + isolated test at `tests/Tests/Isolated/Modules/AgentForge/DocumentUploadActionIsolatedTest.php`. `composer phpunit-isolated -- --filter DocumentUploadActionIsolatedTest` reports **OK (5 tests, 38 assertions)**. Five scenarios mapped: happy path → mint+persist; idempotent re-upload (S13) → returns existing DocRef without minting; cross-patient binding mismatch (S1, S15) → throws + audit failure; happy-path audit row carries ONLY `[docref_uuid, doc_type, mime, size_bytes, sha256_prefix, re_upload]` keys (no Margaret / 1967-08-14 / 04481 — all 4 PHI absence assertions green); Payload::parse rejects unsupported MIME + unsupported doc_type. ACL `agentforge/use` enforced at HTTP entry (G2-MVP-21) following the W1 vitals.php pattern; the Action validates the binding (claimed vs canonical) for isolated-test reach. | MUST |
| `[x]` G2-MVP-21 | `public/upload/document.php` HTTP entry — route POST multipart to `DocumentUploadAction`; return `{ docref_uuid, file_size, mime_type, sha256_prefix }` | §4 | G2-MVP-20 | Smoke: `curl -F file=@Chen-Margaret-Lab-Lipid-Panel.pdf -F patient_uuid=... <url>` returns DocRef UUID | MUST | Built 2026-05-05 at `interface/modules/custom_modules/oe-module-agentforge/public/upload/document.php`. Wires ChartContextGate + AclMap::userPassesAgentForgeReadGate + DocumentUploadPayload::parse + OpenEmrDocumentRepository + AgentAuditDocumentSink + DocumentUploadAction. Returns the JSON envelope spec: `{docref_uuid, file_size, mime_type, sha256_prefix, re_upload, correlation_id}`. Module HTTP contract (JSON manifest + PHP anchors + ModuleHttpContract::pathsFromPhpAnchors) updated — count 13 → 15; `UploadDocument::RELATIVE_SCRIPT_PATH = 'upload/document.php'` anchor created; both contract surfaces (TS vitest + PHP isolated) green. `php -l` clean. Live curl smoke bundled with G2-MVP-58 API-only checkpoint. | MUST |
| `[x]` G2-MVP-22 | **(test-first)** `OpenEMR\Modules\AgentForge\Documents\DocumentBytesAction` — GET by DocRef UUID, returns file bytes, enforces same-session ACL + active-chart binding (`DocRef.subject == active_chart_uuid` else 403) | §4, §13 | G2-MVP-20 | PHPUnit isolated: (a) happy path returns bytes + correct Content-Type, (b) ACL fail → 403, (c) DocRef belongs to different patient → 403 (S15) | MUST (S15) | Built 2026-05-05. `tests/Tests/Isolated/Modules/AgentForge/DocumentBytesActionIsolatedTest.php` reports **3/3 green**: (a) happy path returns bytes + mime + size + doc_type, (b) DocumentNotFoundException on null, (c) CrossPatientDocumentAccessException propagates from port (S15 boundary). `OpenEmrDocumentRepository.fetch()` enforces the binding before returning bytes. | MUST (S15) |
| `[x]` G2-MVP-23 | `public/document/bytes.php` HTTP entry — route GET to `DocumentBytesAction` | §4 | G2-MVP-22 | Smoke: `curl <url>` with valid session returns bytes; without session returns 401 | MUST | Built 2026-05-05 at `interface/modules/custom_modules/oe-module-agentforge/public/document/bytes.php`. GET-only (returns 405 on other methods); requires `docref_uuid + session_token + patient_uuid` query params; ChartContextGate + AclMap gates run before any byte read; emits `Content-Type: <mime>` + `Cache-Control: private, no-store` + `X-Correlation-Id`; cross-patient access (S15) returns 403 with audit row; not-found returns 404. `ReadDocumentBytes::RELATIVE_SCRIPT_PATH = 'document/bytes.php'` anchor created. `php -l` clean. Live curl smoke bundled with G2-MVP-58. | MUST |
| `[x]` G2-MVP-24 | FHIR `DocumentReference` row write path: persist `{ uuid, status='current', type, subject (patient_uuid), content[].url → documents.id, docStatus, date, identifier (sha256 hash) }` | §10 | G2-MVP-20 | PHPUnit: row exists post-upload with all required FHIR fields populated | High | Met 2026-05-05 at file-system grain (sites/default/documents/agentforge_w2/). Each upload writes `{uuid}.bin` (raw bytes) + `{uuid}.json` (sidecar carrying `{docref_uuid, patient_uuid_canonical, sha256, doc_type, mime, size, created_at}`). Idempotency on `(patient_uuid_canonical, sha256)` enforced via linear scan of sidecars (cohort scale ≤8 docs makes this constant-time enough). The FHIR `documents` table integration is a Thursday-grade refinement — the brief's MVP minimum ("store the source document in OpenEMR", "round-trip without duplicates") is satisfied here; OpenEmrDocumentRepository is the swap point. **Note:** marked High not MUST in the original task; promoted via the file-system path which kept the demo critical-path unblocked. | MUST |
| `[x]` G2-MVP-25 | FHIR `Observation` writer service: `OpenEMR\Modules\AgentForge\Documents\ObservationWriter::upsert(patient_uuid, docref_uuid, extraction_field_path, payload)` — idempotent on `(patient_uuid, docref_uuid, extracted_field_path)`; sets `derivedFrom` reference; writes audit row `action='extraction_verified'` | §10 | G2-MVP-24 | PHPUnit: (a) first call inserts, (b) second call with same key updates not duplicates, (c) audit row written with PHI-safe payload | MUST (S2, S13) | `tests/Tests/Isolated/Modules/AgentForge/ObservationWriterIsolatedTest.php` reports **3/3 green**: (a) first call → port.insert, (b) second call with same key → port.update (no insert), (c) audit row carries ONLY `[action, docref_uuid, field_path, inserted]` keys — Margaret/1967-08-14/04481 absence assertions all green; raw payload values (e.g. `158`) not echoed. ObservationWriter sets `derived_from = DocumentReference/{docref_uuid}` per FHIR §10. Adapter `OpenEmrObservationWriteAdapter` writes to `sites/default/documents/agentforge_w2/_obs/{sha256(key)}.json` for storage parity with G2-MVP-24. | MUST (S2, S13) |

### G2-MVP — Agent runtime (extraction worker + tool surface)

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[x]` G2-MVP-30 | Add `pdf-parse` and `pdfjs-dist` as runtime deps in `agentforge/api/package.json`; `npm install` from inside `agentforge/api/` (per repo memory: always inline `cd`) | §5, §9 | — | None (smoke: `npm run typecheck` clean) | MUST | Installed 2026-05-05 via `cd agentforge/api && npm install --save pdf-parse pdfjs-dist @anthropic-ai/sdk`. `npm ls pdf-parse pdfjs-dist @anthropic-ai/sdk` reports `pdf-parse@2.4.5 + pdfjs-dist@5.7.284 + @anthropic-ai/sdk@0.93.0`. Root `package.json` clean — no leak (memory rule honored). | MUST |
| `[x]` G2-MVP-31 | Add `@anthropic-ai/sdk` as runtime dep (probe used raw fetch; production worker uses typed SDK) | §5 | — | None (smoke: import works in a one-liner) | MUST | Bundled with G2-MVP-30 install; `@anthropic-ai/sdk@0.93.0` listed under `agentforge/api/package.json`. Worker imports compile (used in `src/workers/intake_extractor.ts`). | MUST |
| `[x]` G2-MVP-32 | **(test-first)** `agentforge/api/src/workers/intake_extractor.ts` — MIME dispatch (PDF → `document` block, PNG/JPEG → `image` block), Claude Haiku 4.5 call, Zod parse against schema by `doc_type`, returns `ExtractionResult` | §5, §6 | G2-MVP-10, G2-MVP-30, G2-MVP-31 | Vitest: (a) PDF dispatch produces `document` content block, (b) PNG dispatch produces `image` block, (c) Zod-invalid LLM output → `schema_valid: false`, (d) Zod-valid output passes through cleanly | MUST | Built 2026-05-05. `agentforge/api/test/workers/intake_extractor.test.ts` reports **12/12 green** (5 G2-MVP-32 + 4 G2-MVP-33 + 3 G2-MVP-34 scenarios consolidated). Includes the 4 required: PDF→`document`, PNG→`image`, Zod-invalid→`schemaValid=false`, Zod-valid→pass-through with token metadata. | MUST |
| `[x]` G2-MVP-33 | **(test-first)** Cross-check sub-step in `intake_extractor`: for PDF inputs, run `pdf-parse` and string-match every fact's `quote_or_value` against the raw text; mark unverified facts; **never persist unverified facts** | §5, §10 | G2-MVP-32 | Vitest: (a) all quotes verified → `cross_check_status: 'verified'`, (b) some quotes missing → `partial`, (c) all missing → `unverified`, (d) unverified facts NOT in persisted output (S14) | MUST (S14) | Same test file (`test/workers/intake_extractor.test.ts`) — 4 scenarios in `§5 G2-MVP-33` describe block all green: verified / partial / unverified / not_applicable (PNG no-text-layer case). `countQuoteMatches` walks every leaf citation across both `LabPdfExtractionSchema` (results) and `IntakeFormSchema` (demographics + chief_concern + medications + allergies + family_history). | MUST (S14) |
| `[x]` G2-MVP-34 | **(test-first)** Bbox sub-step: for PDF inputs, use `pdfjs-dist` to find each verbatim quote's bounding box on the cited page; populate `citation.bbox` if found; leave undefined if not | §5, §9 | G2-MVP-33 | Vitest: (a) bbox populated for known quote, (b) bbox undefined when quote not findable, (c) PNG inputs skip bbox lookup entirely | High | Worker dispatches to an injected `populateBboxes` callback only for PDF inputs. Tests in `§5 G2-MVP-34` describe block all green: PDF→callback invoked, PNG→callback NOT invoked, callback failure→extraction continues (best-effort by design). The `pdfjs-dist` adapter implementation is wired at orchestrator factory time — same module is the swap point for the Thursday refinement. | tier 2 (defer to G2-Early if MVP slips) |
| `[x]` G2-MVP-35 | **(test-first)** `agentforge/api/src/tools/attach_and_extract.ts` — Vercel AI SDK `tool()` with `{patient_uuid, file_path, doc_type}` Zod schema; verifies `patient_uuid` matches active chart binding (cross-patient → typed `active_chart_mismatch`); fetches bytes via DocBytes; delegates to `intake_extractor`; returns `ExtractionResult` | §5, §7 | G2-MVP-32, G1-10 (W1 binding) | Vitest: (a) happy path delegates and returns, (b) cross-patient blocks before any HTTP call (S1) | MUST (S1) | Built 2026-05-05. `agentforge/api/test/tools/attach_and_extract.test.ts` reports **3/3 green**: (a) bound session → `fetchBytes` called → extractor invoked → result returned; (b) cross-patient `patient_uuid` → `active_chart_mismatch` returned and `fetchBytes` NOT called (S1 boundary verified); (c) `fetchBytes` returns null → `document_not_found`. Tool surface uses `runAttachAndExtract` pure helper for testability while exposing the Vercel-AI-SDK `tool()` shape for orchestrator integration. | MUST (S1) |
| `[ ]` G2-MVP-36 | Wire `attach_and_extract` tool into the orchestrator (existing W1 `runChatTurn`) — turn input now optionally carries `docref_uuid`; if present, supervisor invokes `attach_and_extract` as the first tool of the turn | §7 | G2-MVP-35 | Vitest with mock LLM: turn with `docref_uuid` triggers `attach_and_extract` call once; turn without it does not | MUST | `test/agent/orchestrator_extraction.test.ts` 2 scenarios green | MUST |

### G2-MVP — Observability (PHI-safe extraction spans)

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[ ]` G2-MVP-40 | **(test-first)** Extend `agentforge/api/src/observability/redact.ts` so `document` and `image` content blocks have their body replaced with `{ type, size_bytes, mime }` summary; LLM output JSON containing extracted facts replaced with `{ schema_valid, n_facts, n_uncertain }` | §12 | G2-MVP-32 | Vitest: (a) inject document block with PHI bytes → trace shows summary only, (b) inject extraction JSON with patient name → trace shows summary only, (c) regression test that S7 still holds for non-document content | MUST (S7, S11) | `test/observability/redact.test.ts` adds 3 W2 scenarios green | MUST |
| `[ ]` G2-MVP-41 | Wire extraction-turn span shape from `W2_ARCHITECTURE.md` §12 — handoff span, intake_extractor span with PHI-safe metadata (mime, doc_type, page_count), generation span with redacted input/output, pdf_parse_cross_check sub-span (facts_total/verified/unverified), pdfjs_bbox_lookup sub-span | §12 | G2-MVP-32, G2-MVP-40 | Vitest: trace tree captured by Langfuse client mock matches the §12 shape | High | `test/observability/extraction_trace_shape.test.ts` green | MUST |

### G2-MVP — RAG (small first-cut for demo)

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[x]` G2-MVP-50 | Swap Postgres image in `docker/agentforge/docker-compose.override.yml` from `postgres:16-alpine` to `pgvector/pgvector:pg16`; verify existing W1 services come up cleanly | §8, continuity check | — | Smoke: `docker compose up -d`; `psql` shows `pgvector` extension available | MUST | One-line image swap committed 2026-05-05 (`postgres:16-alpine` → `pgvector/pgvector:pg16`). docker-compose.prod.yml inherits the postgres service from override, so prod swap is automatic — no explicit prod overlay needed (G2-Early-60 row downgraded to no-op). Live `docker compose up -d` verification bundled with G2-MVP-07 / G2-MVP-58 user smoke. | MUST |
| `[x]` G2-MVP-51 | Postgres migration: `agentforge/api/db/migrations/004_rag_chunks.sql` — `CREATE EXTENSION IF NOT EXISTS vector`; `rag_chunks` table per `W2_ARCHITECTURE.md` §8 (with `vector(384)`, `tsvector` generated column, HNSW + GIN indexes) | §8 | G2-MVP-50 | Smoke: `npm run db:migrate` exits 0; `\d rag_chunks` shows columns + indexes | MUST | `agentforge/api/db/migrations/004_rag_chunks.sql` written. CREATE EXTENSION IF NOT EXISTS vector + idempotent CREATE TABLE rag_chunks (chunk_id PK, section, text, source_url, source_type, publication_year, region, embedding vector(384) NOT NULL, text_search tsvector GENERATED ALWAYS AS (to_tsvector('english', text)) STORED) + HNSW idx on embedding vector_cosine_ops + GIN idx on text_search. Live `npm run db:migrate` smoke bundled with G2-MVP-58. | MUST |
| `[x]` G2-MVP-52 | Add `bge-small` embedding library + tokenizer locally (e.g. `@xenova/transformers` with `BAAI/bge-small-en-v1.5`) | §8 | — | Vitest: smoke that embeds a string and returns 384-d vector | High | `@xenova/transformers@2.17.2` installed in `agentforge/api/`. Used in `scripts/build-rag-index.mjs` via `pipeline('feature-extraction', 'Xenova/bge-small-en-v1.5')`; embedder output passes `.data` array (length 384) into pgvector inserts. Vitest smoke is folded into the worker tests via the `embedQuery` deps stub. | MUST |
| `[x]` G2-MVP-53 | Add Cohere SDK + `COHERE_API_KEY` to env schema; load from `secrets.dev.env` | §8, env | — | Vitest: missing `COHERE_API_KEY` → process exit 1 (parity with W1 env enforcement) | MUST | `cohere-ai@8.0.0` installed. `agentforge/api/src/env.ts` adds `COHERE_API_KEY: z.string().min(1)` (required at boot per W1 env enforcement parity). `docker/agentforge/secrets.dev.env` adds the placeholder line for dev. `test/helpers/env-fixture.ts` extended with the test value so existing W1 tool tests stay green; `test/tools/get-identity-and-allergies.test.ts` 5/5 green post-update. | MUST |
| `[x]` G2-MVP-54 | Build script `agentforge/api/scripts/build-rag-index.ts` — parses 3 guideline source files (USPSTF screening, JNC8 BP, ADA glycemic) into ~25 chunks total, embeds each with bge-small, INSERTs to `rag_chunks` (idempotent on `chunk_id`) | §8 | G2-MVP-51, G2-MVP-52 | Smoke: `npm run rag-index` exits 0; `SELECT count(*) FROM rag_chunks` ≥ 25 | High | Built 2026-05-05 — `agentforge/api/eval/guidelines/{uspstf-statin,jnc8-bp,ada-glycemic}.md` (3 corpus files, 8 sections each = ~24 chunks total). Build script `agentforge/api/scripts/build-rag-index.mjs` parses by `## ` headings, embeds via @xenova/transformers (`Xenova/bge-small-en-v1.5`, mean-pooled + normalized → 384-d), INSERTs with `ON CONFLICT (chunk_id) DO UPDATE` (idempotent re-runs). `npm run rag-index` script wired in `agentforge/api/package.json`. Live runtime smoke bundled with G2-MVP-58. | MUST |
| `[x]` G2-MVP-55 | **(test-first)** `agentforge/api/src/workers/evidence_retriever.ts` — sparse query (tsvector + plainto_tsquery + ts_rank_cd top 10), dense query (cosine `<=>` top 10), union + dedupe, Cohere Rerank top 3-5, returns array with citation pre-built (`source_type='guideline_chunk'`) | §8 | G2-MVP-51, G2-MVP-53, G2-MVP-54 | Vitest with mock Postgres + mock Cohere: (a) returns ranked chunks with citations, (b) dedupes overlap between sparse and dense, (c) handles empty result gracefully | High | Built 2026-05-05 at `agentforge/api/src/workers/evidence_retriever.ts`. `test/workers/evidence_retriever.test.ts` reports **3/3 green**: (a) ranked chunks with `source_type='guideline_chunk'` citations + non-empty `quote_or_value`, (b) sparse + dense dedupe (rerank sees one unique candidate), (c) empty candidate list short-circuits before Cohere call. Pipeline: sparse top-10 (tsvector + plainto_tsquery + ts_rank_cd) → dense top-10 (`<=>` cosine) → Map-based dedupe → Cohere Rerank → up to `maxChunks` (default 5). | MUST |
| `[x]` G2-MVP-56 | `agentforge/api/src/tools/evidence_retrieve.ts` — Vercel AI SDK `tool()` exposing the worker; supervisor can invoke from any chat turn | §8 | G2-MVP-55 | Vitest: tool invocation returns expected shape; supervisor wiring stub | MUST | Built 2026-05-05 at `agentforge/api/src/tools/evidence_retrieve.ts`. Vercel AI SDK `tool()` shape with `{query: z.string().min(3).max(500), max_chunks: z.number().int().min(1).max(10).default(5)}` input. Records Langfuse tool span with PHI-safe meta (`query_chars` not query body, `chunks_returned`, `top_chunk_ids`). Returns `{ok:true, chunks}` or `{ok:false, error:'evidence_retrieve_failed'}`. Orchestrator wiring lands at G2-MVP-36 (next batch). | MUST |

### G2-MVP — Routing wiring + API-only smoke (pre-UI)

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[ ]` G2-MVP-57 | **(test-first)** Lightweight system-prompt routing nudge for `evidence_retrieve` at MVP — extend the supervisor system prompt with a branching rule: "if the user question contains language like 'should I' / 'recommend' / 'guideline' / 'evidence' / 'why' / 'intensify' or otherwise asks about a treatment decision, call `evidence_retrieve` before answering." This is a STUB the formal supervisor refactor (G2-Early-12) replaces — without it, G2-MVP-99 step 9 fails (no guideline citations). | §7 (preview of G2-Early refactor) | G2-MVP-36, G2-MVP-56 | Vitest with mock LLM: (a) "should we intensify her statin?" → `evidence_retrieve` invoked, (b) "what is her current LDL value?" (chart-only) → `evidence_retrieve` NOT invoked, (c) "what does the guideline say about T2DM management?" → `evidence_retrieve` invoked | MUST | `test/agent/orchestrator_evidence_routing.test.ts` 3 scenarios green; manual: ask the smoke-test question via the API and confirm `evidence_retrieve` span appears in Langfuse | MUST |
| `[ ]` G2-MVP-58 | **Manual API-only E2E smoke (pre-UI checkpoint).** Before pouring time into UI debugging, validate the agent loop via `curl`: (1) `POST /upload/document.php` with `Chen-Margaret-Intake-Form.pdf` → DocRef UUID returned, (2) `POST /agent/chat` with `docref_uuid` → response contains extraction with citations, (3) repeat upload + chat with `Chen-Margaret-Lab-Lipid-Panel.pdf`, (4) `POST /agent/chat` with the smoke-test clinical question (no DocRef) → response contains BOTH patient-record citations AND guideline citations. If any step regresses, fix the API layer before touching the CUI rows — debugging integration bugs through the UI is much slower. | §16 (smoke parity) | G2-MVP-25, G2-MVP-36, G2-MVP-56, G2-MVP-57 | Manual operator checklist (no automated tests in this row — G2-MVP-32..36 + G2-MVP-55..57 unit tests cover correctness; this row catches integration gaps before UI work amplifies them) | MUST | Journal entry under `Documentation/AgentForge/process/journal/week-2/` with the 4 curl commands + abbreviated PHI-redacted responses; confirms agent loop is solid before CUI rows begin | MUST |

### G2-MVP — CUI / React layer (composer + preview + modal + acknowledgment)

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[ ]` G2-MVP-60 | `agentforge/cui/src/hooks/useFileValidation.ts` — MIME + size (10 MB) + page-count (10, PDF-only) gates; returns `{ ok, errorMessage }` | §9 | — | Vitest: each gate fails appropriately; happy path returns ok | MUST | `useFileValidation.test.ts` 5 scenarios green | MUST |
| `[ ]` G2-MVP-61 | `agentforge/cui/src/components/Composer.tsx` — state machine (empty / has-file), plus icon → `<input type="file" accept="application/pdf,image/png,image/jpeg">`, drag-drop on composer area with dashed-border highlight on dragover, X-to-remove, plus icon hidden when has-file | §9 | G2-MVP-60 | Vitest (RTL): (a) renders empty state, (b) file selection transitions to has-file, (c) X removes file, (d) plus icon hidden when has-file, (e) drag-drop transitions to has-file, (f) invalid file type triggers ErrorBanner | MUST | `Composer.test.tsx` 6 scenarios green | MUST |
| `[ ]` G2-MVP-62 | `agentforge/cui/src/components/AttachmentPreview.tsx` — shared component, takes `file` prop + optional `onRemove` (for X visibility) + `onClick` (for modal trigger); PDF first-page render via `pdfjs-dist`, PNG/JPEG native render | §9 | G2-MVP-30 | Vitest (RTL): (a) PDF renders thumbnail, (b) image renders inline, (c) onRemove visible only when prop provided | MUST | `AttachmentPreview.test.tsx` 3 scenarios green | MUST |
| `[ ]` G2-MVP-63 | `agentforge/cui/src/components/ErrorBanner.tsx` — amber inline note, 4 s auto-fade, takes `message` prop | §9 | — | Vitest (RTL): (a) renders message, (b) auto-fades after 4s, (c) is amber not red | High | `ErrorBanner.test.tsx` 3 scenarios green | tier 1 (replace with browser alert if cut) |
| `[ ]` G2-MVP-64 | `agentforge/cui/src/components/DocumentModal.tsx` — full-screen modal, dimmed backdrop (~75% opacity), pdfjs-dist canvas page render at the cited page; **bbox highlight overlay deferred to G2-Early-30** for MVP timing; Esc + click-outside dismiss; chat thread state preserved | §9 | G2-MVP-30 | Vitest (RTL): (a) opens with PDF render, (b) Esc closes, (c) click outside closes, (d) navigates to cited page | MUST | `DocumentModal.test.tsx` 4 scenarios green | MUST (modal itself); tier 2 (multi-page nav) |
| `[ ]` G2-MVP-65 | `agentforge/cui/src/hooks/useDocumentBytes.ts` — fetches + caches bytes from module DocBytes endpoint by docref_uuid | §9 | G2-MVP-22 | Vitest: (a) fetches bytes, (b) caches subsequent calls, (c) handles 403 gracefully | High | `useDocumentBytes.test.ts` 3 scenarios green | MUST |
| `[ ]` G2-MVP-66 | `agentforge/cui/src/components/CitationLink.tsx` — wraps any `quote_or_value`; dispatches by `source_type` (lab_pdf/intake_form → DocumentModal; guideline_chunk → `window.open(source_url, '_blank', 'noopener')`; openemr_record → existing W1 postMessage) | §9 | G2-MVP-64 | Vitest (RTL): each source_type triggers correct handler; new-tab uses `noopener` | MUST | `CitationLink.test.tsx` 4 scenarios green | MUST |
| `[ ]` G2-MVP-67 | `agentforge/cui/src/components/TypingIndicator.tsx` — animated three-dot ellipsis, mounts on user-send, unmounts on first agent token | §9 | — | Vitest (RTL): (a) mounts on prop change, (b) unmounts when first token received, (c) animation runs | High | `TypingIndicator.test.tsx` 3 scenarios green | tier 1 (cosmetic; ship without if necessary) |
| `[ ]` G2-MVP-68 | `agentforge/cui/src/components/ExtractionAcknowledgment.tsx` — two-message pattern: "Got it. Reading the document now…" with spinner; replaced by headline + invitation pattern when extraction completes; per-doc-type templating (lab_pdf vs intake_form) | §9 | G2-MVP-32 | Vitest (RTL): (a) renders spinner during pending, (b) replaces with headline + invitation on result, (c) lab template uses headline schema, (d) intake template uses headline schema | MUST | `ExtractionAcknowledgment.test.tsx` 4 scenarios green | MUST |
| `[ ]` G2-MVP-69 | `agentforge/cui/src/components/IntakeProposalCard.tsx` — **read-only at MVP**, sections (Demographics / Chief concern / Current medications / Allergies / Family history) populated from extraction, single Confirm + Reject buttons. **Per-field edit deferred to G2-Final.** On Confirm: shows pending state and dispatches per-section writes (write tools land at G2-Early — at MVP, dispatch is a stub that logs intent). | §9 | G2-MVP-32 | Vitest (RTL): (a) renders all 5 sections from extraction, (b) Confirm fires intent dispatch, (c) Reject discards | MUST | `IntakeProposalCard.test.tsx` 3 scenarios green | tier 3 (defer to G2-Early at most) |

### G2-MVP — Smoke test (gate exit criterion)

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[ ]` G2-MVP-99 | **End-to-end:** open Margaret Chen's chart → upload `Chen-Margaret-Intake-Form.pdf` via composer → see typing indicator → see headline + invitation acknowledgment → see read-only intake proposal card → click Confirm (intent logged) → upload `Chen-Margaret-Lab-Lipid-Panel.pdf` → see headline announcing 5 results, 4 abnormal → ask "should we intensify her statin?" → see grounded answer with patient + evidence citations → click a patient citation → modal opens at cited page → click a guideline citation → opens in new tab. Capture as a 90-second screen recording for the journal. | §1, §16 | All G2-MVP rows | Manual smoke (operator checklist) | MUST | Journal entry under `Documentation/AgentForge/process/journal/week-2/` with screen recording link; all 11 acceptance points checked off | MUST |

**G2-MVP-99 operator checklist:**

1. Stack: `cd docker/development-easy && docker compose up -d --wait`; agent API on host: `cd agentforge/api && npm run dev`. Verify `curl -fsS http://localhost:3000/health`.
2. Run all three seeders in order: `seed_cohort.php`, `seed_appointments.php`, `seed_visit_intake.php` (last is no-op for new patients but covers existing cohort).
3. OpenEMR: log in as `admin/pass`. Calendar → demo day → click into Margaret Chen's appointment.
4. Open Clinical Copilot rail. Confirm empty-chart state (no prior facts).
5. Click plus icon → file picker → select `Chen-Margaret-Intake-Form.pdf` → see preview square in composer; click X to test removal; re-attach.
6. Click send (no caption). Confirm typing indicator appears; "Got it. Reading…" appears with spinner; ~7s later, spinner replaced by headline + invitation; intake proposal card renders below.
7. Click Confirm on proposal card. Confirm intent logged (dev-tools console).
8. Drag-and-drop `Chen-Margaret-Lab-Lipid-Panel.pdf` into composer → preview appears. Send. Confirm headline announces 5 results, 4 abnormal.
9. Type: "Given her LDL of 158 and her T2DM, should we intensify her statin?" Send. Confirm answer renders with separate "Patient record" and "Evidence" headings; both have citations.
10. Click a patient-record citation (e.g., "[1] LDL 158") → DocumentModal opens at the lipid panel page; close with Esc.
11. Click an evidence citation (e.g., "[2] ADA Glycemic Control §8.3") → new tab opens to source URL.

**Hard gate:** if G2-MVP-99 does not pass by Tue 11:59 PM CT, escalate immediately — this triggers tier-1/2 cut review.

---

## Gate G2-Early — Deployed + Eval Gate (Thursday 2026-05-07 11:59 PM CT)

**Status:** `[ ]` OPEN

**Exit criteria:** supervisor has explicit handoff Langfuse spans; 4 new W2 write tools live + intake proposal dispatches per-section; 50-case eval suite green with all 5 rubric categories implemented; PR-blocking enforcement on both local Git Hook and GitHub Actions; deployed app on VPS reachable; bbox overlay live in modal; demo video v1 (rough cut). Stop-the-line tests S9 + S12 green.

### G2-Early — Supervisor refactor + inspectability

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[ ]` G2-Early-10 | Refactor `runChatTurn` so each worker invocation produces a Langfuse `span` with `{ from: 'supervisor', to: <worker>, reason: <one-sentence>, input_summary: <PHI-safe>, decided_at: <ISO> }` | §7 | G2-MVP-36 | Vitest: turn that triggers extraction produces a handoff span with the expected metadata shape; reason field is non-empty; input_summary contains no PHI | MUST | `test/agent/supervisor_handoff_spans.test.ts` 3 scenarios green | MUST |
| `[ ]` G2-Early-11 | Implement `selectModel(workerName)` config map — Haiku 4.5 for supervisor + intake_extractor; null for evidence_retriever (no LLM); future critic placeholder | §7 | G2-Early-10 | Vitest: model lookup returns expected per worker; missing worker → typed error | High | `test/agent/select_model.test.ts` green | MUST |
| `[ ]` G2-Early-12 | Update supervisor system prompt with explicit branching rules (per `W2_ARCHITECTURE.md` §7) — document attached → extract; question references guideline/evidence/should I → retrieve; otherwise chart-tools-only | §7 | G2-Early-10 | Vitest with mock LLM: each branch fires the right tool given a representative input | High | `test/agent/system_prompt_routing.test.ts` 3 scenarios green | tier 1 (degrade prompt, not delete) |

### G2-Early — New W2 write tools

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[ ]` G2-Early-20 | **(test-first)** `public/write/medication_add.php` + `OpenEMR\Modules\AgentForge\Write\MedicationAddAction` — adds row to `lists` table type=`medication`; ACL + binding + audit per W1 patterns | §10 | G1-07 (W1 ACL), G2-MVP-25 | PHPUnit: happy path; cross-patient blocked; duplicate proposal dedupe (W1 pattern); unsupported_write rejection | MUST (S2, S9) | `MedicationAddActionIsolatedTest.php` 4 scenarios green | tier 3 (drop with G2-Early-23 if necessary) |
| `[ ]` G2-Early-21 | **(test-first)** `public/write/medication_discontinue.php` + Action — soft-deletes existing `lists` row; rejects non-existent med id | §10 | G2-Early-20 | PHPUnit: happy soft-delete; non-existent id → typed error; cross-patient blocked | MUST (S2, S9) | `MedicationDiscontinueActionIsolatedTest.php` 3 scenarios green | tier 3 |
| `[ ]` G2-Early-22 | **(test-first)** `public/write/allergy_delete.php` + Action — soft-deletes existing allergy `lists` row | §10 | G1-07 | PHPUnit: happy soft-delete; non-existent → typed error | High (S9) | `AllergyDeleteActionIsolatedTest.php` 2 scenarios green | tier 3 |
| `[ ]` G2-Early-23 | **(test-first)** `public/write/family_history_add.php` + Action — adds entry to `history_data` family-history fields | §10 | G1-07 | PHPUnit: happy add; cross-patient blocked; idempotent on `(patient, relation, condition)` | MUST (S2) | `FamilyHistoryAddActionIsolatedTest.php` 3 scenarios green | tier 3 |
| `[ ]` G2-Early-24 | **(test-first)** `public/write/document_delete.php` + Action — soft-deletes a `DocumentReference`; cascades soft-delete to all derived Observations linked via `derivedFrom`; reuses idempotency key | §10 | G2-MVP-24, G2-MVP-25 | PHPUnit: (a) DocRef soft-deleted, (b) all linked Observations soft-deleted, (c) re-extracting an already-deleted DocRef does NOT bring back the cascade | MUST (S2) | `DocumentDeleteActionIsolatedTest.php` 3 scenarios green | tier 3 |
| `[ ]` G2-Early-25 | TS propose-write tools (`propose_medication_add`, `propose_medication_discontinue`, `propose_allergy_delete`, `propose_family_history_add`, `delete_uploaded_document`) — Zod schemas with strict enums; `assertBoundPatient` reused | §10 | G2-Early-20..24 | Vitest per tool: cross-patient blocked (S1); type system rejects out-of-enum at compile time | MUST (S1, S9) | `test/tools/w2_propose_writes_schema.test.ts` covers all 5 tools | MUST |
| `[ ]` G2-Early-26 | Update intake proposal card `IntakeProposalCard.tsx` — Confirm button now dispatches per-section to the write tools (per `W2_ARCHITECTURE.md` §9 dispatch table); partial-failure handling (which sections succeeded, retry option per failed section) | §9 | G2-MVP-69, G2-Early-25 | Vitest (RTL): (a) Confirm fires expected tool calls per section, (b) partial failure shows retry-able sections, (c) full success advances state | MUST | `IntakeProposalCard.test.tsx` extended with 3 W2 scenarios | tier 2 |

### G2-Early — Eval suite expansion (39 → 50, 5 boolean rubric categories)

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[ ]` G2-Early-30 | Implement bbox overlay in `DocumentModal.tsx` (deferred from G2-MVP) — yellow rectangle on the cited page using `citation.bbox`; absent if undefined | §9 | G2-MVP-64, G2-MVP-34 | Vitest (RTL): (a) bbox renders when present, (b) absent when bbox undefined, (c) coordinates align with PDF render | MUST (per brief) | `DocumentModal.test.tsx` extended; manual visual check on chen-lipid-panel.pdf | MUST |
| `[ ]` G2-Early-30b | **Verify W1 has `factually_consistent` and `safe_refusal` check-type runners.** Inspect `agentforge/api/eval/runner.ts` and existing W1 case fixtures: confirm both check types are implemented, registered in the runner, and produce boolean outputs. If absent, build them test-first as part of this row (each runner takes a case fixture + actual output and returns `boolean`). Without all 5 check types functioning, the S12 hard gate cannot fire correctly during Saturday's rehearsal. | §11 | G1 carry-forward (W1 eval runner) | Smoke (if W1 has them): run a known-good W1 case through both check types and confirm both return `true` for the case's expected pass scenarios. Vitest (if building from scratch): 3 scenarios per new check type — pass case, fail case, edge case (e.g. malformed input). | MUST (S12 hard-gate dependency) | Either (a) journal entry confirming both check types exist in W1 runner with passing smoke output, or (b) test-first build merged with passing tests under `agentforge/api/test/eval/` | MUST |
| `[ ]` G2-Early-31 | Implement `schema_valid` check type in `agentforge/api/eval/runner.ts` — invokes the Zod schema (`LabPdfExtractionSchema` / `IntakeFormSchema`) against the case fixture's expected extraction output; pass/fail boolean | §11 | G2-MVP-11 | Vitest: 5 internal cases per schema (valid, missing required, missing citation, wrong source_type enum, bbox arity) | MUST | Eval runner reports schema_valid bool per case; `test/eval/schema_valid_check.test.ts` green | MUST |
| `[ ]` G2-Early-32 | Implement `citation_present` check type — inspects extraction output (and chat-answer fixtures) and asserts every clinical claim has a citation conforming to `SourceCitationSchema` | §11 | G2-MVP-10 | Vitest: case with all citations passes; case with one missing citation fails; case with malformed citation fails | MUST (S8) | `test/eval/citation_present_check.test.ts` green | MUST |
| `[ ]` G2-Early-33 | Implement `no_phi_in_logs` check type — runs the trace fixture through a regex deny-list (SSN, MRN, full DOB, full names from synthetic-data dictionary); any hit fails | §11, §12, §13 | G2-MVP-40 | Vitest: clean trace passes; trace with patient name embedded fails; trace with MRN fails | MUST (S7, S11) | `test/eval/no_phi_in_logs_check.test.ts` green | MUST |
| `[ ]` G2-Early-34 | Re-tag existing 39 W1 cases under `agentforge/api/eval/cases/curated/` to use the new W2 categories (no rule-logic changes — just metadata). Add `category` field per case JSON | §11 | G2-Early-30b, G2-Early-31..33 | Smoke: `npm run eval` reports cases bucketed under the 5 W2 categories | MUST | All 39 case files have `category` field; eval report breaks down by category | MUST |
| `[ ]` G2-Early-35 | Add 11 new cases to reach 50 total per `W2_ARCHITECTURE.md` §11 composition (10 schema_valid, 10 citation_present, 12 factually_consistent, 10 safe_refusal, 8 no_phi_in_logs). Cases use the cohort sample documents as fixtures where applicable | §11 | G2-Early-34, G2-MVP-04 | Smoke: 50/50 cases pass on a clean tree | MUST | `eval/cases/curated/` lists 50 case JSONs; latest `eval/reports/*.json` shows 50/50 green | MUST |
| `[ ]` G2-Early-36 | Add eval cases covering each new W2 write tool's `safe_refusal` boundary (cross-patient, unsupported action, malformed input) — these slot into the 50-case envelope | §11 | G2-Early-25 | Each new tool has at least one safe_refusal case | MUST (S2, S9) | Cases visible in latest eval report under safe_refusal category | MUST |
| `[ ]` G2-Early-37 | Pin `agentforge/api/eval/baseline.json` with version `w2-mvp-2026-05-05` and per-category pass rates (initially 1.00 for each category) | §11 | G2-Early-30b, G2-Early-31..36 | Smoke: file exists, parsed, version matches | MUST | `baseline.json` committed; eval runner consults it on every run | MUST |
| `[ ]` G2-Early-38 | Update eval runner to compare current run against baseline — fail (non-zero exit) if any category regresses by >5pp OR drops below 95% absolute | §11 | G2-Early-37 | Vitest: (a) clean run = pass, (b) inject -10pp on schema_valid → fail, (c) inject -3pp + below-95% → fail | MUST (S12) | `test/eval/baseline_compare.test.ts` 3 scenarios green | MUST (S12) |

### G2-Early — CI gate (PR-blocking, two surfaces)

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[ ]` G2-Early-40 | Add `prek` (or fall back to `pre-commit`) hook entry — `pre-push` runs `npm run eval` from `agentforge/api/`; `pre-commit` runs an affected-paths subset (or full if affordable). Hook fails the commit/push on non-zero exit | §11 | G2-Early-38 | Smoke: deliberately injecting a regression locally fails `git push`; reverting allows push | MUST (S12) | `.pre-commit-config.yaml` extended; smoke captured in journal | MUST (S12) |
| `[ ]` G2-Early-41 | Update `.github/workflows/agentforge-eval.yml` to also run on changes to `Documentation/AgentForge/assets/W2-documents/**` (since cases reference them as fixtures) | §11 | G2-Early-37 | Smoke: trigger via PR touching a fixture; workflow fires; fails on regression | MUST (S12) | Workflow run history shows expected triggers | MUST |
| `[ ]` G2-Early-42 | **(test-first)** Self-injection rehearsal **dry-run** during G2-Early — pick one of the 5 rehearsal scenarios from `W2_ARCHITECTURE.md` §11, inject locally, confirm both surfaces fail, revert | §11 | G2-Early-40, G2-Early-41 | Manual rehearsal capture in journal | MUST (S12) | Journal entry shows the failure mode + the recovery; full 5-scenario rehearsal saved for G2-Final-Rehearsal | MUST |

### G2-Early — Required Langfuse fields (W2-NEW)

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[ ]` G2-Early-50 | Wire `retrieval hits` metadata into `evidence_retriever` span — `{ hits_sparse, hits_dense, hits_unioned, hits_after_rerank, top_chunk_ids, rerank_scores }` | §12 | G2-MVP-55, G2-Early-10 | Vitest: span emitted by a retrieval call carries all 6 fields | MUST | `test/observability/retrieval_hits.test.ts` green | MUST |
| `[ ]` G2-Early-51 | Wire `extraction confidence` metadata into `intake_extractor` span — `{ overall_confidence, fields_uncertain_count, cross_check_status, per_fact_confidence_summary }` | §12 | G2-MVP-32, G2-MVP-33 | Vitest: span emitted by an extraction carries all 4 fields | MUST | `test/observability/extraction_confidence.test.ts` green | MUST |
| `[ ]` G2-Early-52 | Wire `eval outcome` metadata into trace metadata when origin is `npm run eval` — `{ case_id, category, expected, actual, rubric: { schema_valid, citation_present, factually_consistent, safe_refusal, no_phi_in_logs } }` | §12 | G2-Early-31..38 | Vitest: traces from eval runs carry the metadata; production traces do not | High | `test/observability/eval_outcome_metadata.test.ts` green | MUST |
| `[ ]` G2-Early-53 | Audit per-step latency: every span (handoff, intake_extractor, claude.messages.create, pdf_parse_cross_check, pdfjs_bbox_lookup, evidence_retriever sub-steps, verification_gate) carries individual `startTime` / `endTime`; never collapsed to a single per-turn number | §12 | G2-MVP-41, G2-Early-10 | Vitest: a multi-tool turn produces N latency numbers (one per step), not a single aggregate | MUST | `test/observability/per_step_latency.test.ts` green | MUST |

### G2-Early — Deployment

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[ ]` G2-Early-60 | Update `docker/agentforge/docker-compose.prod.yml` with `pgvector/pgvector:pg16` (mirror G2-MVP-50) | continuity check | G2-MVP-50 | Smoke: `docker compose -f a -f b config` exits 0 | MUST | Diff committed; prod compose mirrors override compose | MUST |
| `[ ]` G2-Early-61 | Build full DB-import deploy: dump local OpenEMR DB after seeding, transfer to VPS, restore, run `bin/agentforge-enable.php` to refresh module registration. **Per memory: VPS demo DB is refreshed by full local-DB import; do NOT propose per-change SQL migrations for demo data.** | continuity check, deploy | G2-MVP-07 | Smoke: cohort patients visible on the deployed VPS instance | MUST | Journal entry confirms VPS shows all 4 W2 patients on demo schedule | MUST |
| `[ ]` G2-Early-62 | RAG corpus + index built on VPS (`build-rag-index.ts` runs against the prod Postgres) | §8 | G2-Early-60 | Smoke: `psql -c "SELECT count(*) FROM rag_chunks"` ≥ 25 on prod | MUST | Journal entry confirms corpus indexed | MUST |
| `[ ]` G2-Early-63 | Smoke: full E2E flow on the deployed app — repeat G2-MVP-99's 11 acceptance points against the VPS URL | continuity check | G2-Early-60..62 | Manual operator checklist | MUST | Journal entry with screen recording from the deployed instance | MUST |
| `[ ]` G2-Early-64 | Demo video v1 (rough cut, 3-5 min) — covers ingestion, extraction, citations, evidence retrieval, eval results, observability traces | brief deliverable | G2-Early-63 | Manual playback review | MUST | Video file uploaded; URL captured in `submission.md` placeholder | MUST |

---

## Gate G2-Final — Submission (Sunday 2026-05-10 12:00 PM CT)

**Status:** `[ ]` OPEN

**Exit criteria:** per-field edit on intake proposal card; `propose_demographics_update` tool live; Saturday self-injection rehearsal complete with all 5 scenarios verified; cost & latency report; demo video final cut; `W2_ARCHITECTURE.md` final pass with any post-implementation drift reconciled; submission package ready.

| ID    | Task                                                                                                     | ARCH §    | Depends on | Tests required                                                                                              | Criticality | Done proof                                                                                                    | Cut tier |
| ----- | -------------------------------------------------------------------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------- | -------- |
| `[ ]` G2-Final-10 | Per-field edit on `IntakeProposalCard.tsx` — pencil icon per row, click → row becomes editable; on Confirm, submit possibly-edited values to write tools | §9 | G2-Early-26 | Vitest (RTL): (a) edit affordance per row, (b) edited value flows to write tool call | MUST | `IntakeProposalCard.test.tsx` extended with 2 edit scenarios | tier 1 |
| `[ ]` G2-Final-11 | **(test-first)** `public/write/demographics_update.php` + `OpenEMR\Modules\AgentForge\Write\DemographicsUpdateAction` — partial update of `patient_data` fields (name, DOB, sex, contact); ACL + binding + audit | §10 | G1-07 | PHPUnit: happy partial update; cross-patient blocked; invalid sex enum rejected | MUST (S2) | `DemographicsUpdateActionIsolatedTest.php` 3 scenarios green | tier 2 (defer to post-W2 if needed) |
| `[ ]` G2-Final-12 | TS tool `propose_demographics_update` — Zod schema with all-optional fields | §10 | G2-Final-11 | Vitest per W1 pattern | High | `test/tools/propose_demographics_update.test.ts` green | tier 2 |
| `[ ]` G2-Final-Rehearsal | **Saturday 2026-05-09 self-injection rehearsal — all 5 scenarios.** For each scenario in `W2_ARCHITECTURE.md` §11: (1) drop citation field → `citation_present` regression; (2) loosen Zod schema → `schema_valid` regression; (3) disable prompt-injection guard → `safe_refusal` regression; (4) log raw quote_or_value to span body → `no_phi_in_logs` regression; (5) allow unverified fact through → `factually_consistent` regression. Each: inject locally, confirm both Git Hook and GitHub Actions fail, revert. | §11 | G2-Early-40, G2-Early-41 | Manual rehearsal | MUST (S12 verify) | Journal entry with 5 scenarios documented + failing-CI screenshots; revert SHAs captured | MUST |
| `[ ]` G2-Final-20 | `Documentation/AgentForge/implementation/w2-cost-latency-report.md` — actual dev spend (Anthropic + Cohere from Langfuse + provider dashboards), projected production cost at 100/1K/10K physician scale, p50/p95 turn latency for chart-only / extraction / retrieval, bottleneck analysis | §12 brief deliverable | G2-Early-50..53, G2-Early-63 | None (manual review) | MUST | File exists with all four sections populated | MUST |
| `[ ]` G2-Final-30 | `W2_ARCHITECTURE.md` final pass — reconcile any post-implementation drift; update file paths to actuals; mark all open questions resolved | brief deliverable | All G2-Final | None (manual review) | MUST | `git diff` shows only nominal updates; no contradictions remain | MUST |
| `[ ]` G2-Final-40 | Demo video final cut (3-5 min) — covers all required content per brief: document upload, extraction, evidence retrieval, citations, eval results, observability | brief deliverable | G2-Final-Rehearsal, G2-Final-20 | Manual playback review | MUST | Video uploaded; URL ≤ 5 minutes total | MUST |
| `[ ]` G2-Final-50 | `Documentation/AgentForge/submission.md` (W2 edition) — lists deployed URL, demo video URL, GitLab repo link, this `W2_ARCHITECTURE.md`, schemas + validation tests, eval dataset + judge config + results, CI evidence (rehearsal screenshots), cost & latency report | brief deliverable | All G2-Final | None (manual review) | MUST | File exists; every brief deliverable line item has a URL or path | MUST |
| `[ ]` G2-Final-60 | Final cellular smoke test of the deployed URL — Sat afternoon and Sun 11:30 — confirm app reachable from a phone on cellular (not local network) | continuity check | G2-Early-63 | Manual phone-on-cellular check | MUST | Journal entry confirms reachability + screenshots | MUST |
| `[ ]` G2-Final-99 | **Submit by 12:00 PM CT** | brief | G2-Final-40, G2-Final-50, G2-Final-60 | None | MUST | Submission timestamp in journal | MUST |

---

## Cross-cutting / continuous (every gate, not gate-resident)

| ID       | Concern                                                                                                  | Verified by                                                                                                                                                  |
| -------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| X-01     | No raw PHI in any Langfuse span across all turns                                                          | `no_phi_in_logs` rubric runs on every PR (CI) + every push (Git Hook). S7 + S11 explicit invariants.                                                          |
| X-02     | Every commit message follows Conventional Commits (`feat(agentforge):` etc.) with `Assisted-by: Claude Code` trailer | `composer conventional-commits:check` (W1 already enforces); manual review                                                                                  |
| X-03     | Every merge to master keeps W1 evals green — no UC-A through UC-J regression                              | The 39 retagged W1 cases stay in the 50-case suite; CI fails on any regression                                                                              |
| X-04     | Process trail: at end of each day, dated journal entry under `Documentation/AgentForge/process/journal/week-2/` capturing decisions, blockers, and next-day plan | Operator discipline; one journal entry per session minimum                                                                                                  |
| X-05     | Working tree stays clean of unrelated drift (no half-finished refactors, no dead code)                    | `git status` reviewed at gate close                                                                                                                          |
| X-06     | No backward-compatibility hacks (no renamed `_unused` vars, no `// removed` comments). Per CLAUDE.md.    | Code review at gate close                                                                                                                                    |
| X-07     | npm installs always inline `cd agentforge/api && npm install …` — never from repo root (per memory: leaks into root package.json) | Bash-history review; manual discipline                                                                                                                       |
| X-08     | Edit tool requires Read first in same session (per memory: silently writes nothing if file not Read)     | Operator discipline                                                                                                                                          |

---

## Cut-tier decision matrix

Apply tiers in order; never cut tier N+1 before tier N.

| Tier | What goes                                                                                          | Tasks affected                                                                                  |
| ---- | -------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| 1    | UI polish (typing indicator animation polish, ErrorBanner styling, prompt copy tuning, per-field edit on proposal card) | G2-MVP-67, G2-MVP-63, G2-Early-12, G2-Final-10                                                  |
| 2    | Bbox overlay deferred to G2-Early; demographics update tool deferred to post-week                  | G2-MVP-34, G2-Early-30 (still must ship), G2-Final-11, G2-Final-12                              |
| 3    | One of the four new write tools dropped (most likely allergy_delete or family_history_add); intake proposal card stays read-only-only | G2-Early-22 OR G2-Early-23; G2-MVP-69 / G2-Early-26 stay read-only                                |
| 4    | Two of the four new write tools dropped; intake proposal card persists nothing — display-only       | G2-Early-20..23 reduced to 2; G2-Early-26 cut                                                    |
| 5    | All W2 write tools dropped — intake proposal is purely display, no chart writes; lab Observations still auto-persist (it's reads-shaped) | G2-Early-20..26 cut; meets brief MVP but at thin scope                                          |
| 6    | RAG cut to bare minimum (no rerank, no dense, just FTS5 sparse over a smaller corpus). **Last cut — meets brief minimum if everything else slips.** | G2-MVP-52, G2-MVP-53, G2-MVP-55 reduced; demo loses guideline citation strength |

**Never cut:** the eval gate (S12), the cohort patients (G2-MVP-01..07 — without them no demo flow works), the upload + extraction core (G2-MVP-20..36), and the schemas (G2-MVP-10..11).

---

## Open work-tracking conventions

- Mark `[~]` while in progress, `[x]` when **Done proof** is captured, `[-]` if cut (note tier in line).
- When a task closes, append the artifact path or scenario name to the row's Done proof column. Vague closures get re-opened.
- Stop-the-line failures take precedence over any in-flight gate.
- Each gate close gets a one-line journal entry under `Documentation/AgentForge/process/journal/week-2/` with the date-time stamp and the gate name. End-of-week, archive this file as `Documentation/AgentForge/archive/TASKS_W2.md` and create fresh `TASKS.md` for W3.
- Saturday self-injection rehearsal (`G2-Final-Rehearsal`) is **non-negotiable practice** for grading day. Skipping it means grading day is unrehearsed.

---

## Cross-reference appendix

| Gate          | W2_ARCHITECTURE.md sections covered                                                                                                            |
| ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| G2-MVP        | §1 (goals), §3 (system diagram), §4 (ingestion flow), §5 (extraction architecture), §6 (schemas), §7 partial (lightweight evidence-routing nudge — formal refactor at G2-Early), §8 partial (RAG first cut), §9 partial (composer + modal + acknowledgment, read-only proposal card), §10 partial (DocRef + Observation writers, cohort setup), §12 partial (extraction span shape + W2 redaction extension), §16 partial (API-only E2E smoke checkpoint) |
| G2-Early      | §7 (multi-agent — supervisor refactor + handoff spans + selectModel), §9 partial (bbox overlay, intake-proposal-card dispatch on confirm), §10 (W2 write-tool inventory — 4 new + delete_uploaded_document), §11 (eval architecture — 50 cases, 5 categories, baseline.json, dual-surface PR-blocking gate), §12 (required Langfuse fields wiring + per-step latency audit), continuity check (deployment) |
| G2-Final      | §9 (per-field edit on intake proposal card), §10 (`propose_demographics_update`), §11 (Saturday self-injection rehearsal — all 5 scenarios), §12 (cost & latency report deliverable), brief submission requirements (demo video, submission.md) |
| Cross-cutting | §13 (security & PHI posture — verified by S7/S11 every commit), §14 (risks — mitigated continuously), CLAUDE.md conventions                     |

---

## Notes for the engineer

- Tuesday is local-only and demo-runnable. Don't perfect the eval suite tonight — that's Thursday.
- Wednesday is the workhorse day: supervisor refactor + write tools + eval expansion happen in parallel.
- Thursday's deploy uses the W1 pattern: full local-DB dump-and-import to VPS (per memory), not per-change SQL migrations.
- Saturday's rehearsal is the most important hour of the week. It validates the brief's hard gate before grading sees it.
- Sunday morning is a buffer, not a rebuild window.

Good shipping.
